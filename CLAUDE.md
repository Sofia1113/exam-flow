# CLAUDE.md

本文件是 Claude Code 在该仓库中工作时必须遵守的规则。

## 项目简介

exam-flow —— 考试流程管理系统(政企风格),包含考试报名、安排、成绩管理等流程。

## 设计规范(强制)

**本项目的所有 UI 修改必须遵守 [`DESIGN.md`](./DESIGN.md) 中定义的设计系统。**

`DESIGN.md` 是唯一的视觉规范来源,采用政企网站风格(政务红 #C7000B + 深藏青 #1F4E79)。进行任何 UI 相关改动(新建页面、修改样式、新增组件、调整布局)前,必须先通读该文件。

### 硬性规则

1. **颜色**:只能使用 `DESIGN.md` front matter 中 `colors` 定义的色值,禁止引入 palette 之外的色值(品牌红 #C7000B 是唯一强调色)。
2. **字体**:标题/按钮用黑体(Noto Sans SC / 微软雅黑)700,正文用宋体(Noto Serif SC / SimSun)400。禁止 emoji、书法体、圆体。
3. **形状**:圆角 ≤ 4px,默认 0px 直角;投影几乎不用,层级靠 1px 细线与表面色表达。
4. **布局**:版心 1200px 居中,间距为 4px 倍数,区块标题用"左侧 4px 红竖线 + 黑体标题"模式。
5. **文案**:简体中文,官方庄重语气,不出现网络流行语;页脚必须保留备案号等法定信息。

### 修改规范的流程

- 新增组件 / 调整视觉 token 时,先更新 `DESIGN.md` 再改代码,保证文档与实现同步。
- 完成 UI 改动后,对照 `DESIGN.md` 末尾的**一致性自查清单**逐条核对,未通过不视为完成。

### 冲突处理

- 任何样式冲突(组件库默认样式、第三方样式、框架约定与本规范冲突时)一律以 `DESIGN.md` 为准。
- 若任务要求与 `DESIGN.md` 冲突,先向用户说明冲突点,获得明确授权后再偏离规范。
